import styled from 'styled-components';

const Grid = styled.div`
  display: grid;
  grid-template-columns: auto auto auto;
  grid-gap: 50px;
  padding: 25px 0px;
  overflow: auto;
`;

export default Grid;
